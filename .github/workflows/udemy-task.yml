name: Udemy Task 

on: 
#   push:
  workflow_dispatch:

jobs:
  pra1:
    runs-on: ubuntu-latest
    steps:
      - name: Hello_world
        run: |
          echo 'Hello_World!'
  pra2:
    runs-on: ubuntu-latest
    steps:
      - name: goodbye
        run: |
          echo 'Goodbye! with exit0'  
          exit 0
      - name: exit
        run: echo "I have exit0"