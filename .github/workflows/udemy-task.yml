name: Udemy Task 

on: 
#   push:
  workflow_dispatch:
#   pull_request:
  schedule:
    - cron: '45 13 * * *'

jobs:
#   pra1:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Hello_world
#         run: |
#           echo 'Hello_World!'
#           echo "$RUNNER_OS"
#   pra2:
#     runs-on: ubuntu-latest
#     steps:
#       - name: goodbye
#         run: |
#           echo 'Goodbye! with exit0'  
#           exit 0
#       - name: exit
#         # shell: bash
#         run: |
#           echo "I have exit0"
#           echo "$RUNNER_OS"
  pra3:
    runs-on: ubuntu-latest
    steps:
      - name: event-print
        uses: actions/checkout@v5
      - name: runs
        run: |
          ls
          echo "${{github.event_name}}"
          echo "$RUNNER_OS"
      - name: install deps
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - name:
        run: |
          cd using_actions_react/react-app/
          npm ci
      - name: run tests
        run: |
          cd using_actions_react/react-app/
          npm run test