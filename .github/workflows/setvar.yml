name: Set var test

on: [push]

jobs:
  var-job:
    runs-on: ubuntu-latest
    steps:
      - name: set-var
        run: |
          echo "MY_VAL = Hello_mars" >> $GITHUB_ENV
      - name: display-var
        run: |
          echo "$MY_VAL"